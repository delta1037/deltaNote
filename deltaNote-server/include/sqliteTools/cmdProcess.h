#ifndef SQLITE_TOOLS_CMD_PROCESS
#define SQLITE_TOOLS_CMD_PROCESS

#include <string>

bool isExit(std::string str);

bool isShow(std::string str);

bool isUsers(std::string str);

bool isTable(std::string str);

#endif // SQLITE_TOOLS_CMD_PROCESS